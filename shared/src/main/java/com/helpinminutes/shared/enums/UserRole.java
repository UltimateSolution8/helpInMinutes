package com.helpinminutes.shared.enums;

/**
 * Enum representing user roles in the system.
 * Used across multiple services for authorization and access control.
 */
public enum UserRole {
    BUYER,
    HELPER,
    ADMIN
}
